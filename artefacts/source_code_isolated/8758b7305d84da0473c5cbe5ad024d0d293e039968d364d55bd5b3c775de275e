public void recordInitializeEvent(int numScans, int numNetworks){
    synchronized (mLock) {
        if (!mIsInSession) {
            return;
        }
        mCurrentSession.recordInitializeEvent(numScans, numNetworks, SystemClock.elapsedRealtime());
    }
}