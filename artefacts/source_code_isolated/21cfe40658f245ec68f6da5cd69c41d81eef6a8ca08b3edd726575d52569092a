public void answer(){
    Log.startSession("RC.an", getActiveOwnerInfo());
    try {
        if (mConnected) {
            mConnectionService.answer(mConnectionId, Log.getExternalSession(mCallingPackageAbbreviation));
        }
    } catch (RemoteException ignored) {
    } finally {
        Log.endSession();
    }
}