public boolean isInLegacyMode(){
    String mode = SystemProperties.get(SYSTEM_PROPERTIES_IWLAN_OPERATION_MODE);
    return mode.equals(IWLAN_OPERATION_MODE_LEGACY) || mPhone.getHalVersion().less(RIL.RADIO_HAL_VERSION_1_4);
}