public boolean equals(Object obj){
    if (!(obj instanceof IpRange)) {
        return false;
    }
    final IpRange other = (IpRange) obj;
    return Arrays.equals(mStartAddr, other.mStartAddr) && Arrays.equals(mEndAddr, other.mEndAddr);
}