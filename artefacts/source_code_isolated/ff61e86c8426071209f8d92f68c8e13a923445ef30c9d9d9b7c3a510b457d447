public String getTag(String prefix){
    try {
        return ActivityManager.getService().getTagForIntentSender(mTarget, prefix);
    } catch (RemoteException e) {
        throw e.rethrowFromSystemServer();
    }
}