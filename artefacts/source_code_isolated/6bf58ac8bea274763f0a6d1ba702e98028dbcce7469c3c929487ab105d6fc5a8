public void abort(){
    Log.startSession("RC.a", getActiveOwnerInfo());
    try {
        if (mConnected) {
            mConnectionService.abort(mConnectionId, Log.getExternalSession(mCallingPackageAbbreviation));
        }
    } catch (RemoteException ignored) {
    } finally {
        Log.endSession();
    }
}