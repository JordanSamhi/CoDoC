public InlinePresentationSpec build(){
    checkNotUsed();
    mBuilderFieldsSet |= 0x8;
    if ((mBuilderFieldsSet & 0x4) == 0) {
        mStyle = defaultStyle();
    }
    InlinePresentationSpec o = new InlinePresentationSpec(mMinSize, mMaxSize, mStyle);
    return o;
}