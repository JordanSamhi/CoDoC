public void recordDiscoverySessionDuration(long creationTime, boolean isPublish){
    synchronized (mLock) {
        MetricsUtils.addValueToLogHistogram(mClock.getElapsedSinceBootMillis() - creationTime, isPublish ? mHistogramPublishDuration : mHistogramSubscribeDuration, DURATION_LOG_HISTOGRAM);
    }
}