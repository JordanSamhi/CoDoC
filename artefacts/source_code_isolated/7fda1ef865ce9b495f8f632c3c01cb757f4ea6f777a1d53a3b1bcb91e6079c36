public static boolean isSupportedServiceType(@NonNull NsdServiceInfo serviceInfo, @NonNull Set<String> serviceTypes){
    String curType = serviceInfo.getServiceType().toLowerCase();
    for (String type : serviceTypes) {
        if (curType.contains(type.toLowerCase())) {
            return true;
        }
    }
    return false;
}