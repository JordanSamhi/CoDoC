public int hashCode(){
    if (hashCode == 0) {
        prepareJavaRepresentation();
        int hash = 0;
        for (int i = 0; i < numberLength; ++i) {
            hash = hash * 33 + digits[i];
        }
        hashCode = hash * sign;
    }
    return hashCode;
}