public boolean up(){
    if (isWithinCurrentMonth(mRow - 1, mColumn)) {
        mRow--;
        return false;
    }
    previousMonth();
    mRow = 5;
    while (!isWithinCurrentMonth(mRow, mColumn)) {
        mRow--;
    }
    return true;
}