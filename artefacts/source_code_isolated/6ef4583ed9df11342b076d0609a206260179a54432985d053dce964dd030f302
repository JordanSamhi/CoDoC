public static SliceItem find(Slice s, String type, String[] hints, String[] nonHints){
    List<String> h = s.getHints();
    return find(new SliceItem(s, SliceItem.FORMAT_SLICE, null, h.toArray(new String[h.size()])), type, hints, nonHints);
}