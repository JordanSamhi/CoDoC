public static T trimToLengthWithEllipsis(@Nullable T text, @IntRange(from = 1) int size){
    T trimmed = trimToSize(text, size);
    if (trimmed.length() < text.length()) {
        trimmed = (T) (trimmed.toString() + "...");
    }
    return trimmed;
}