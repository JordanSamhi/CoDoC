public static List<StoreFile> createSharedFiles(boolean shouldEncryptCredentials){
    return createFiles(Environment.getWifiSharedDirectory(), Arrays.asList(STORE_FILE_SHARED_GENERAL, STORE_FILE_SHARED_SOFTAP), UserHandle.ALL, shouldEncryptCredentials);
}