public byte[] readRawData() throws IOException{
    byte[] bytes = null;
    try {
        bytes = mAtomicFile.readFully();
    } catch (FileNotFoundException e) {
        return null;
    }
    return bytes;
}