public static void dispose(){
    synchronized (AnimationThread.class) {
        if (sInstance == null) {
            return;
        }
        getHandler().runWithScissors(() -> sInstance.quit(), 0);
        sInstance = null;
    }
}