public String toString(){
    return "nanoAppId: 0x" + Long.toHexString(mAppId) + ", nanoAppVersion: 0x" + Integer.toHexString(mAppVersion) + ", versionMask: " + mVersionRestrictionMask + ", vendorMask: " + mAppIdVendorMask;
}