public void subtractObscuredTouchableRegion(Region touchableRegion, View view){
    if (!mIsExpanded || mShowingManage || (mManageEducationView != null && mManageEducationView.getVisibility() == VISIBLE)) {
        touchableRegion.setEmpty();
    }
}