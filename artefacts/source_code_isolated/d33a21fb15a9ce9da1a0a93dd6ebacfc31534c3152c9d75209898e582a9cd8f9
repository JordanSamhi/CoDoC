public boolean equals(Object o){
    if (!(o instanceof Partition)) {
        return false;
    }
    Partition op = (Partition) o;
    return mName.equals(op.mName) && mFingerprint.equals(op.mFingerprint) && mTimeMs == op.mTimeMs;
}