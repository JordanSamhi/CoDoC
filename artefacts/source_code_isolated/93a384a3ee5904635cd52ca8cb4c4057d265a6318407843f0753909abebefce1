public static File createNewFile(File file){
    try {
        file.createNewFile();
    } catch (IOException e) {
        Slog.w(TAG, "Failed to create file:" + file.getAbsolutePath(), e);
    }
    return file;
}