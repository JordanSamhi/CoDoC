public static void assertLogcat(String tag, int... logs){
    assertThat(ShadowLog.getLogsForTag(tag).stream().map(logItem -> logItem.type).collect(toSet())).named("Log items (specified per level)").containsExactly(IntStream.of(logs).boxed().toArray());
}