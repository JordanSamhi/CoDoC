public boolean endBatchEdit(){
    synchronized (this) {
        if (mBatchEditNesting > 0) {
            mTextView.endBatchEdit();
            mBatchEditNesting--;
            return true;
        }
    }
    return false;
}