public boolean equals(Object o){
    if (this == o)
        return true;
    if (o == null || getClass() != o.getClass())
        return false;
    @SuppressWarnings("unchecked")
    OverlayableInfo that = (OverlayableInfo) o;
    return true && Objects.equals(name, that.name) && Objects.equals(actor, that.actor);
}