public boolean equals(@Nullable Object o){
    if (o == null || !(o instanceof CallAttributes) || hashCode() != o.hashCode()) {
        return false;
    }
    if (this == o) {
        return true;
    }
    CallAttributes s = (CallAttributes) o;
    return (Objects.equals(mPreciseCallState, s.mPreciseCallState) && mNetworkType == s.mNetworkType && Objects.equals(mCallQuality, s.mCallQuality));
}