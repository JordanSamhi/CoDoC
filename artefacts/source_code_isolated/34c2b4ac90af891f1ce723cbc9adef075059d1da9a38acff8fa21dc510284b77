public void refresh(){
    setTitle(mWifiEntry.getTitle());
    final int level = mWifiEntry.getLevel();
    if (level != mLevel) {
        mLevel = level;
        updateIcon(mLevel);
        notifyChanged();
    }
    setSummary(mWifiEntry.getSummary(false));
    mContentDescription = buildContentDescription();
}