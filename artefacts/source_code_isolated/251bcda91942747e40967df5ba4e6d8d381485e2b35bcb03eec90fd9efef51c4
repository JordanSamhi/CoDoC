public static List<SliceItem> findAll(SliceItem s, String type, String[] hints, String[] nonHints){
    return stream(s).filter(item -> compareTypes(item, type) && (item.hasHints(hints) && !item.hasAnyHints(nonHints))).collect(Collectors.toList());
}