public static String getMimeType(@Nullable String path, int formatCode){
    String mimeType = getMimeTypeForFile(path);
    if (!MIME_TYPE_DEFAULT.equals(mimeType)) {
        return mimeType;
    }
    return getMimeTypeForFormatCode(formatCode);
}