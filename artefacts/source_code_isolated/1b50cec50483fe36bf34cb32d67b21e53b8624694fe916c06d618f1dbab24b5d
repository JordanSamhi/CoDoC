public void reset(){
    mSeenQueue.addAll(mPendingQueue);
    mPendingQueue.clear();
    mCheckpointTag = -1;
    Queue<LogMaker> tmp = mPendingQueue;
    mPendingQueue = mSeenQueue;
    mSeenQueue = tmp;
}