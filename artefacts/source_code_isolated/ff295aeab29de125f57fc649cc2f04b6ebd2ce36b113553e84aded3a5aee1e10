public DataRemovalRequest build(){
    throwIfDestroyed();
    Preconditions.checkState(mForEverything || mLocusIds != null, "must call either #forEverything() or add one #addLocusId()");
    mDestroyed = true;
    return new DataRemovalRequest(this);
}