public boolean isGroupConversation(){
    if (mBuilder != null && mBuilder.mContext.getApplicationInfo().targetSdkVersion < Build.VERSION_CODES.P) {
        return mConversationTitle != null;
    }
    return mIsGroupConversation;
}