public Uri getServiceUri(@CapabilityFlag long type){
    Uri result = mServiceMap.getOrDefault(type, null);
    if (result == null) {
        return isCapable(type) ? getContactUri() : null;
    }
    return result;
}