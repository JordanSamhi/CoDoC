public void clearText(){
    Editable text;
    text = mLetters.getText();
    text.clear();
    text = mDigits.getText();
    text.clear();
    if (mIsQwerty) {
        setMode(DIGITS_AND_LETTERS);
    } else {
        setMode(DIGITS_ONLY);
    }
}