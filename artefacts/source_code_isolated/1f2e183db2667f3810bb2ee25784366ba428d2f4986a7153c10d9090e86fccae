public void cancel(){
    if (mStarted && !mEnded) {
        fireAnimationEnd();
        mEnded = true;
        guard.close();
    }
    mStartTime = Long.MIN_VALUE;
    mMore = mOneMoreTime = false;
}