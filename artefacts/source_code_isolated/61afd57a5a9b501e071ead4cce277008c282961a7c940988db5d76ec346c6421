public int getCurrentTransport(@ApnType int apnType){
    if (isInLegacyMode()) {
        return AccessNetworkConstants.TRANSPORT_TYPE_WWAN;
    }
    return mCurrentTransports.get(apnType) == null ? AccessNetworkConstants.TRANSPORT_TYPE_WWAN : mCurrentTransports.get(apnType);
}