public void unhold(){
    Log.startSession("RC.u", getActiveOwnerInfo());
    try {
        if (mConnected) {
            mConnectionService.unhold(mConnectionId, Log.getExternalSession(mCallingPackageAbbreviation));
        }
    } catch (RemoteException ignored) {
    } finally {
        Log.endSession();
    }
}