public void start(){
    synchronized (mPending) {
        if (!mStarted) {
            if (mPending.size() > 0) {
                mHandler.post(mWorker);
            }
            mStarted = true;
        }
    }
}