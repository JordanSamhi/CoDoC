public boolean logSettingsTileClick(String logKey, int sourceMetricsCategory){
    if (TextUtils.isEmpty(logKey)) {
        return false;
    }
    action(sourceMetricsCategory, MetricsEvent.ACTION_SETTINGS_TILE_CLICK, SettingsEnums.PAGE_UNKNOWN, logKey, 0);
    return true;
}