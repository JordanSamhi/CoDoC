public void set(@NonNull Outline src){
    mMode = src.mMode;
    if (src.mMode == MODE_PATH) {
        if (mPath == null) {
            mPath = new Path();
        }
        mPath.set(src.mPath);
    }
    mRect.set(src.mRect);
    mRadius = src.mRadius;
    mAlpha = src.mAlpha;
}