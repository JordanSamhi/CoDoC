public void setGroupDividerEnabled(boolean groupDividerEnabled){
    if (mGroupDivider != null) {
        mGroupDivider.setVisibility(!mHasListDivider && groupDividerEnabled ? View.VISIBLE : View.GONE);
    }
}