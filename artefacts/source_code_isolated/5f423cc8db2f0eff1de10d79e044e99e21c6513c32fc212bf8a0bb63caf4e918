public int getUtilizationRatio(int frequency){
    if (mContext.getResources().getBoolean(R.bool.config_wifiChannelUtilizationOverrideEnabled)) {
        if (ScanResult.is24GHz(frequency)) {
            return mContext.getResources().getInteger(R.integer.config_wifiChannelUtilizationOverride2g);
        }
        if (ScanResult.is5GHz(frequency)) {
            return mContext.getResources().getInteger(R.integer.config_wifiChannelUtilizationOverride5g);
        }
        return mContext.getResources().getInteger(R.integer.config_wifiChannelUtilizationOverride6g);
    }
    return mChannelUtilizationMap.get(frequency, BssLoad.INVALID);
}