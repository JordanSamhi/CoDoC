public static boolean getBooleanThemeValue(@NonNull RenderResources resources, @NonNull ResourceReference attr, boolean defaultValue){
    ResourceValue value = resources.findItemInTheme(attr);
    value = resources.resolveResValue(value);
    if (value == null) {
        return defaultValue;
    }
    return XmlUtils.convertValueToBoolean(value.getValue(), defaultValue);
}