public void remove(@NonNull E entry){
    if (entry == null) {
        return;
    }
    int index = mLinkedList.indexOf(entry);
    if (index < 0) {
        return;
    }
    mLinkedList.remove(index);
}