public void noteValidationSuccess(@NonNull ExtendedWifiInfo wifiInfo){
    if (mValidatedThisConnectionAtLeastOnce)
        return;
    updatePerBssid(Event.VALIDATION_SUCCESS, wifiInfo);
    mValidatedThisConnectionAtLeastOnce = true;
    doWrites();
}