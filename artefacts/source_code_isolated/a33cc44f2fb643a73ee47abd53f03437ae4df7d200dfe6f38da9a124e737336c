public String toString(){
    return String.format("ConnectionRequest %s %s isAdhocConf: %s", mAddress == null ? Uri.EMPTY : Connection.toLogSafePhoneNumber(mAddress.toString()), bundleToString(mExtras), isAdhocConferenceCall() ? "Y" : "N");
}