public void recordAttachSessionDuration(long creationTime){
    synchronized (mLock) {
        MetricsUtils.addValueToLogHistogram(mClock.getElapsedSinceBootMillis() - creationTime, mHistogramAttachDuration, DURATION_LOG_HISTOGRAM);
    }
}