public String toString(){
    final String appearanceString = ViewDebug.flagsToString(InsetsFlags.class, "appearance", mAppearance);
    return "AppearanceRegion{" + appearanceString + " bounds=" + mBounds.toShortString() + "}";
}