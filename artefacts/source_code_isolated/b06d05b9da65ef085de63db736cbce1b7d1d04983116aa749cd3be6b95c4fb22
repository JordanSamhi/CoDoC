public void onSessionCommand(final Controller2Link caller, final int seq, final Session2Command command, final Bundle args, ResultReceiver resultReceiver){
    mSession.onSessionCommand(caller, seq, command, args, resultReceiver);
}