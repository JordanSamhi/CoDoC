public void reset(){
    isSimplePath = true;
    mLastDirection = null;
    if (rects != null)
        rects.setEmpty();
    final FillType fillType = getFillType();
    nReset(mNativePath);
    setFillType(fillType);
}