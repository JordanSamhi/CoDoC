public void connect(){
    if (DBG)
        log("connect");
    if (!isSupported()) {
        logw("connect: not supported.");
        return;
    }
    mRetryCount = 0;
    post(mGetServiceRunnable);
}