public Builder setAtomId(final int atomId){
    if (0 == mAtomId) {
        mAtomId = atomId;
        if (1 == mNumElements) {
            writeInt(atomId);
        } else {
            mErrorMask |= ERROR_ATOM_ID_INVALID_POSITION;
        }
    }
    return this;
}