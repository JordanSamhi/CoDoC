public void retireBroadcastLocked(final BroadcastRecord r){
    if (r != mCurrentBroadcast) {
        Slog.wtf(TAG, "Retiring broadcast " + r + " doesn't match current outgoing " + mCurrentBroadcast);
    }
    mCurrentBroadcast = null;
}