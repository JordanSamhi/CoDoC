public String getUniqueName(){
    int guess = 1;
    while (true) {
        String name = mContext.getString(getNameTemplateResource(), guess);
        if (isUnique(name)) {
            return name;
        }
        guess++;
    }
}