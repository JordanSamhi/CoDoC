public float getStarRating(){
    float ratingValue = -1.0f;
    switch(mRatingStyle) {
        case RATING_3_STARS:
        case RATING_4_STARS:
        case RATING_5_STARS:
            if (isRated()) {
                ratingValue = mRatingValue;
            }
    }
    return ratingValue;
}