public long getOutputStallDuration(@NonNull final Class<T> klass, @NonNull final Size size){
    if (mSupportsPrivate) {
        return mRecommendedMap.getOutputStallDuration(klass, size);
    }
    return 0;
}