public boolean equals(final MeteringRectangle other){
    if (other == null) {
        return false;
    }
    return (mX == other.mX && mY == other.mY && mWidth == other.mWidth && mHeight == other.mHeight && mWeight == other.mWeight);
}