public static String certificateToPemString(@Nullable X509Certificate cert) throws IOException, CertificateEncodingException{
    if (cert == null) {
        return EMPTY_CERT;
    }
    return new String(Credentials.convertToPem(cert), StandardCharsets.US_ASCII);
}