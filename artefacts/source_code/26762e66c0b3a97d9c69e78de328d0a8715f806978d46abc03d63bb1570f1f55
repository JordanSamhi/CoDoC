public void disconnect(){
    Log.startSession("RC.d", getActiveOwnerInfo());
    try {
        if (mConnected) {
            mConnectionService.disconnect(mConnectionId, Log.getExternalSession(mCallingPackageAbbreviation));
        }
    } catch (RemoteException ignored) {
    } finally {
        Log.endSession();
    }
}