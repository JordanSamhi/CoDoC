public static ImageReader newInstance(@IntRange(from = 1) int width, @IntRange(from = 1) int height, @Format int format, @IntRange(from = 1) int maxImages, @Usage long usage){
    return new ImageReader(width, height, format, maxImages, usage);
}