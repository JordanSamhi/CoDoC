public boolean checkCallingOrSelfCanGetSmscAddress(String callingPackage, String message){
    if (!isCallerDefaultSmsPackage(callingPackage)) {
        TelephonyPermissions.enforeceCallingOrSelfReadPrivilegedPhoneStatePermissionOrCarrierPrivilege(mContext, mPhone.getSubId(), message);
    }
    return true;
}