public HandlerThread getRttHandlerThread(){
    if (mRttHandlerThread == null) {
        mRttHandlerThread = new HandlerThread("wifiRttService");
        mRttHandlerThread.start();
    }
    return mRttHandlerThread;
}