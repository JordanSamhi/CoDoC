public void release(){
    if (mMediaPlayer != null) {
        mMediaPlayer.release();
        mMediaPlayer = null;
        prepared = false;
    }
    if (surface != null) {
        surface.release();
        surface = null;
    }
}