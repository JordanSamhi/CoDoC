public void onFrameMetricsAvailable(int dropCountSinceLastInvocation){
    if (mWindow.get() != null) {
        mListener.onFrameMetricsAvailable(mWindow.get(), mFrameMetrics, dropCountSinceLastInvocation);
    }
}