public void flush(){
    synchronized (mAudioLock) {
        mAudioBuffers.clear();
        mCallbackHandler.removeCallbacksAndMessages(null);
    }
    if (mAudioTrack != null) {
        mAudioTrack.pause();
        mAudioTrack.flush();
        mAudioTrack.stop();
    }
    native_flush();
}