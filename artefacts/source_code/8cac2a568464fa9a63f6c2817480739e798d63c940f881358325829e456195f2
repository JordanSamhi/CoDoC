public static Result parse(@NonNull InputStream in, @NonNull String fontDir) throws XmlPullParserException, IOException{
    XmlPullParser parser = Xml.newPullParser();
    parser.setInput(in, null);
    parser.nextTag();
    return readFamilies(parser, fontDir);
}