public List<String> getAllCodePaths(){
    if (getPath() != null) {
        ArrayList<String> list = new ArrayList<>();
        list.add(getPath());
        return list;
    } else {
        return mCodePaths;
    }
}