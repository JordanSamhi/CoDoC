public static void assertEventNotLogged(int tag, Object... values){
    assertThat(ShadowEventLog.getEntries()).named("Event logs").doesNotContain(new ShadowEventLog.Entry(tag, Arrays.asList(values)));
}