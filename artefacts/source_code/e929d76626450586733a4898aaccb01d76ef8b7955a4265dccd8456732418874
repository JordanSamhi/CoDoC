public void onHandoverFailed(@Annotation.NetworkType int srcNetworkType, @Annotation.NetworkType int targetNetworkType, @NonNull ImsReasonInfo reasonInfo){
    try {
        mListener.callSessionHandoverFailed(srcNetworkType, targetNetworkType, reasonInfo);
    } catch (RemoteException e) {
        e.rethrowFromSystemServer();
    }
}