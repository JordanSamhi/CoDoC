public int getLidState(){
    int sw = mInputManager.getSwitchState(-1, InputDevice.SOURCE_ANY, InputManagerService.SW_LID);
    if (sw > 0) {
        return LID_CLOSED;
    } else if (sw == 0) {
        return LID_OPEN;
    } else {
        return LID_ABSENT;
    }
}