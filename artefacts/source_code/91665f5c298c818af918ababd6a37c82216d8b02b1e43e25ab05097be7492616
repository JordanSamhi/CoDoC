public final void sendExtraCommand(int uid, int pid, String command, Bundle extras){
    mExecutor.execute(obtainRunnable(AbstractLocationProvider::onExtraCommand, this, uid, pid, command, extras).recycleOnUse());
}