public int getUserBadgeResId(@UserIdInt int userId){
    try {
        return mService.getUserBadgeResId(userId);
    } catch (RemoteException re) {
        throw re.rethrowFromSystemServer();
    }
}