public static boolean deleteCertificateTypesForAlias(KeyStore keystore, String alias, int uid){
    return keystore.delete(Credentials.USER_CERTIFICATE + alias, uid) & keystore.delete(Credentials.CA_CERTIFICATE + alias, uid);
}