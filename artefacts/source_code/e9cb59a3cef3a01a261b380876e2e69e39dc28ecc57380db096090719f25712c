public Slice.Builder addBundle(Bundle bundle, @Nullable @SliceSubtype String subType, @SliceHint List<String> hints){
    Objects.requireNonNull(bundle);
    mItems.add(new SliceItem(bundle, SliceItem.FORMAT_BUNDLE, subType, hints));
    return this;
}