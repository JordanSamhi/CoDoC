public void close(){
    if (isOpen()) {
        synchronized (mLock) {
            try {
                mChannel.close();
            } catch (Exception e) {
                Log.e(TAG, "Error closing channel", e);
            }
        }
    }
}