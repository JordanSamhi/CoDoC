public static Bitmap createBitmap(@Nullable InputStream input, boolean isMutable, Density density) throws IOException{
    return createBitmap(input, getPremultipliedBitmapCreateFlags(isMutable), density);
}