public void print(char ch){
    synchronized (lock) {
        try {
            appendLocked(ch);
        } catch (IOException e) {
            Log.w("FastPrintWriter", "Write failure", e);
            setError();
        }
    }
}