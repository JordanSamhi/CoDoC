public boolean isWhitelisted(@NonNull String packageName){
    Objects.requireNonNull(packageName);
    if (mWhitelistedPackages == null)
        return false;
    return mWhitelistedPackages.containsKey(packageName) && mWhitelistedPackages.get(packageName) == null;
}