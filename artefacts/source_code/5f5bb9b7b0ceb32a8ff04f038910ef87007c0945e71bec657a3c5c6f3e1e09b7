public void reject(){
    Log.startSession("RC.r", getActiveOwnerInfo());
    try {
        if (mConnected) {
            mConnectionService.reject(mConnectionId, Log.getExternalSession(mCallingPackageAbbreviation));
        }
    } catch (RemoteException ignored) {
    } finally {
        Log.endSession();
    }
}