public void setPath(@NonNull Path path){
    if (path.isEmpty()) {
        setEmpty();
        return;
    }
    if (mPath == null) {
        mPath = new Path();
    }
    mMode = MODE_PATH;
    mPath.set(path);
    mRect.setEmpty();
    mRadius = RADIUS_UNDEFINED;
}