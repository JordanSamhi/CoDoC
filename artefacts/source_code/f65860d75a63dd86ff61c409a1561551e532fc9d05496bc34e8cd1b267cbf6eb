public static PermissionInfo generatePermissionInfo(ParsedPermission p, @PackageManager.ComponentInfoFlags int flags){
    if (p == null)
        return null;
    return PackageInfoWithoutStateUtils.generatePermissionInfo(p, flags);
}