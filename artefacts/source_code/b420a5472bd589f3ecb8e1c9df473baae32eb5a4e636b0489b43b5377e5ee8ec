public void showNotification(@NonNull NotificationEntry entry){
    if (Log.isLoggable(TAG, Log.VERBOSE)) {
        Log.v(TAG, "showNotification");
    }
    addAlertEntry(entry);
    updateNotification(entry.getKey(), true);
    entry.setInterruption();
}