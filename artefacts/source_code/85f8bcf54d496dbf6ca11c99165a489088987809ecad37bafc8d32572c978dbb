public void callSessionMergeComplete(ImsCallSessionImplBase newSession){
    try {
        mListener.callSessionMergeComplete(newSession != null ? newSession.getServiceImpl() : null);
    } catch (RemoteException e) {
        e.rethrowFromSystemServer();
    }
}