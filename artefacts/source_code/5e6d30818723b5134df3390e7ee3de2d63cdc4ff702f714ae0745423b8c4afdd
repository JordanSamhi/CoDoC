public void close(){
    try {
        applyPlayer(new VolumeShaper.Configuration(mId), new Operation.Builder().terminate().build());
    } catch (IllegalStateException ise) {
        ;
    }
    if (mWeakPlayerBase != null) {
        mWeakPlayerBase.clear();
    }
}