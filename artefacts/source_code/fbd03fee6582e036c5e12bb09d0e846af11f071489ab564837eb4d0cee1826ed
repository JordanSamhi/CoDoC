public void onServiceDisconnected(ComponentName name){
    mHandler.post(() -> {
        mService = null;
        if (mCurrentDream == DreamRecord.this) {
            stopDream(true, "service disconnected");
        }
    });
}