public void removeOnWifiUsabilityListener(int listenerIdentifier){
    mOnWifiUsabilityListeners.remove(listenerIdentifier);
    if (DBG) {
        Log.v(TAG, "Removing listener. Num listeners: " + mOnWifiUsabilityListeners.getNumCallbacks());
    }
}