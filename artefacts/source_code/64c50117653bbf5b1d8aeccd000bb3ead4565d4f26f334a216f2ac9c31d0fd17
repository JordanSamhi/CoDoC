public boolean equals(final Object obj){
    if (obj == null) {
        return false;
    } else if (this == obj) {
        return true;
    } else if (obj instanceof OisSample) {
        final OisSample other = (OisSample) obj;
        return mTimestampNs == other.mTimestampNs && mXShift == other.mXShift && mYShift == other.mYShift;
    }
    return false;
}