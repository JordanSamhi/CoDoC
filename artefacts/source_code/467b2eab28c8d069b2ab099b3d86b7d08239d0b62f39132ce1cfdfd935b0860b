public CardState getCardState(){
    synchronized (mLock) {
        if (mCardState == null) {
            return CardState.CARDSTATE_ABSENT;
        } else {
            return mCardState;
        }
    }
}