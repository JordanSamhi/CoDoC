public int getInt(){
    if (length != 4) {
        throw new IllegalArgumentException("Accesing an int from a TLV element of length " + length);
    }
    return peekInt(mRefArray, offset, byteOrder);
}