public void setWifiEnabled(boolean enable){
    mWifiEnabled = enable;
    logd("Set WiFi " + (enable ? "enabled" : "disabled"));
    if (enable) {
        retrieveWifiScanner();
    } else {
        doWrites();
    }
}