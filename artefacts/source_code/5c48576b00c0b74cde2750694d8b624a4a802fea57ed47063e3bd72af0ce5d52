public ProviderRequest build(){
    if (mInterval == Long.MAX_VALUE) {
        return EMPTY_REQUEST;
    } else {
        return new ProviderRequest(true, mInterval, mLowPowerMode, mLocationSettingsIgnored, mLocationRequests, mWorkSource);
    }
}