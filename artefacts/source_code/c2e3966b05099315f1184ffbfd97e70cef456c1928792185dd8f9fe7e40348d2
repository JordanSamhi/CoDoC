public void notifyImeHidden(IBinder windowToken){
    synchronized (mH) {
        try {
            if (mCurMethod != null && mCurRootView != null && mCurRootView.getWindowToken() == windowToken) {
                mCurMethod.notifyImeHidden();
            }
        } catch (RemoteException re) {
        }
    }
}