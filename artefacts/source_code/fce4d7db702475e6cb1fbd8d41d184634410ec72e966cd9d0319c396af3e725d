public void setSurfaceTexture(SurfaceTexture sur, int w, int h){
    validate();
    Surface s = null;
    if (sur != null) {
        s = new Surface(sur);
    }
    mWidth = w;
    mHeight = h;
    nContextSetSurface(w, h, s);
}