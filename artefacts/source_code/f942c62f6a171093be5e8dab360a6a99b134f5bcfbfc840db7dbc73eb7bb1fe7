public final void putHidlMemory(long offset, @NonNull HidlMemory mem){
    putNativeHandle(offset + 0, mem.getHandle());
    putInt64(offset + 16, mem.getSize());
    putString(offset + 24, mem.getName());
}