public void add(float value){
    mTimes[mEnd] = mClock.millis();
    mValues[mEnd] = value;
    if (mCount < mSize) {
        mCount++;
    } else {
        mStart = (mStart + 1) % mSize;
    }
    mEnd = (mEnd + 1) % mSize;
}