public int getCenterFreq0(){
    if (mCenterFreqIndex1 == 0 || mChannelMode == 0) {
        return 0;
    } else {
        return ScanResult.convertChannelToFrequencyMhz(mCenterFreqIndex1, WifiScanner.WIFI_BAND_5_GHZ);
    }
}