public void playDtmfTone(char digit){
    Log.startSession("RC.pDT", getActiveOwnerInfo());
    try {
        if (mConnected) {
            mConnectionService.playDtmfTone(mConnectionId, digit, null);
        }
    } catch (RemoteException ignored) {
    } finally {
        Log.endSession();
    }
}