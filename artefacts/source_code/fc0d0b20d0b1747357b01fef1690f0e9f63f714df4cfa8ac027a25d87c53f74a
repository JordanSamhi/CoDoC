public void onImsExternalCallStateUpdate(List<ImsExternalCallState> externalCallState){
    if (DBG)
        Rlog.d(TAG, "onImsExternalCallStateUpdate");
    if (mListener != null) {
        mListener.onImsExternalCallStateUpdate(externalCallState);
    }
}