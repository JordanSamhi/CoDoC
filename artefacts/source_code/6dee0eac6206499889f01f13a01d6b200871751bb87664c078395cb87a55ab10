public static int resolveContrastColor(Context context, int notificationColor, int backgroundColor){
    return ContrastColorUtil.resolveContrastColor(context, notificationColor, backgroundColor, false);
}