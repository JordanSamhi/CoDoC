public void onSessionFinished(int sessionId){
    scheduleAsyncRequest((s) -> s.onSessionFinished(sessionId));
    writeSessionEvent(sessionId, FrameworkStatsLog.CONTENT_CAPTURE_SESSION_EVENTS__EVENT__ON_SESSION_FINISHED, 0, getComponentName(), null, false);
}