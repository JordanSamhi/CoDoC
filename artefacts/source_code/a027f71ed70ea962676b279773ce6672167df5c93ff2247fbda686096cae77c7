public void toShortStringShortItems(StringBuilder b, boolean first){
    if (mItems.size() > 0) {
        if (!first) {
            b.append(' ');
        }
        for (int i = 0; i < mItems.size(); i++) {
            b.append("{...}");
        }
    }
}