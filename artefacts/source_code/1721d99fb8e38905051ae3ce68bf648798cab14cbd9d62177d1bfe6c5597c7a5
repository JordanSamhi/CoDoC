public void showMenuWithDelay(int menuState, Rect stackBounds, boolean allowMenuTimeout, boolean willResizeMenu, boolean showResizeHandle){
    fadeOutMenu();
    showMenuInternal(menuState, stackBounds, allowMenuTimeout, willResizeMenu, true, showResizeHandle);
}