public boolean requestIcon(long bssid, String fileName){
    if (bssid == 0 || fileName == null)
        return false;
    return mSupplicantHook.requestIcon(mSupplicantHook.getClientInterfaceName(), Utils.macToString(bssid), fileName);
}