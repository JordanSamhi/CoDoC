public long longValue(){
    if (isPosInf()) {
        return Long.MAX_VALUE;
    } else if (isNegInf()) {
        return Long.MIN_VALUE;
    } else if (isNaN()) {
        return 0;
    } else {
        return mNumerator / mDenominator;
    }
}