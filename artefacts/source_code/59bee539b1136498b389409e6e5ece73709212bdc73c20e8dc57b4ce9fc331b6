public void sendSessionCommand(final Controller2Link caller, final int seq, final Session2Command command, final Bundle args, ResultReceiver resultReceiver){
    try {
        mISession.sendSessionCommand(caller, seq, command, args, resultReceiver);
    } catch (RemoteException e) {
        throw new RuntimeException(e);
    }
}